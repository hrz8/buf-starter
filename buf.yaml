version: v2
modules:
  - path: proto
    name: buf.build/hrz8/altalune
deps:
  - buf.build/bufbuild/protovalidate:v0.14.2
lint:
  use:
    - STANDARD
  except:
    - FIELD_LOWER_SNAKE_CASE # Allow more flexible field naming for API consistency
    - ENUM_VALUE_UPPER_SNAKE_CASE # Allow more flexible enum naming
breaking:
  use:
    - FILE
  except:
    - FIELD_WIRE_COMPATIBLE_TYPE # Allow some field type changes during development
